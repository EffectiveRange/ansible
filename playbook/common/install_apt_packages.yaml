---
- name: Install APT packages
  hosts: all
  become: true
  vars:
    packages:
     - "curl"

  tasks:
    - name: Install packages
      ansible.builtin.apt:
        name: "{{ packages }}"
        update_cache: yes
        state: present
